#!/bin/bash

# tmux new-session -d -s e00 "source ~/.bash_profile; conda activate auto; ./scripts/eval.sh 0 567798408"
# tmux new-session -d -s e01 "source ~/.bash_profile; conda activate auto; ./scripts/eval.sh 0 1908781080"
# tmux new-session -d -s e02 "source ~/.bash_profile; conda activate auto; ./scripts/eval.sh 1 322576610"
# tmux new-session -d -s e03 "source ~/.bash_profile; conda activate auto; ./scripts/eval.sh 1 1589076274"
# tmux new-session -d -s e04 "source ~/.bash_profile; conda activate auto; ./scripts/eval.sh 2 436657949"

# tmux kill-session -t e00
# tmux kill-session -t e01
# tmux kill-session -t e02
# tmux kill-session -t e03
# tmux kill-session -t e04
# tmux kill-session -t e05
# tmux kill-session -t e06
# tmux kill-session -t e07
# tmux kill-session -t e08
# tmux kill-session -t e09
# tmux kill-session -t e10
# tmux kill-session -t e11
# tmux kill-session -t e12
# tmux kill-session -t e13
# tmux kill-session -t e14

